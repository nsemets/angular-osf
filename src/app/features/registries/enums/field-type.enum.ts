export enum FieldType {
  Text = 'text',
  TextArea = 'textarea',
  Select = 'select',
  MultiSelect = 'multi-select',
  Checkbox = 'checkbox',
  Radio = 'radio',
  Date = 'date',
  Number = 'number',
  Email = 'email',
  Url = 'url',
}
